import { useContext } from "../contextManager.js";
export function emitLoggerFile(emitterOptions, srcPath = "src") {
    var _a, _b, _c;
    const project = useContext("outputProject");
    if (emitterOptions.options.flavor !== "azure") {
        return;
    }
    const sourceFile = project.createSourceFile(`${srcPath}/logger.ts`, "", {
        overwrite: true
    });
    sourceFile.addImportDeclaration({
        namedImports: ["createClientLogger"],
        moduleSpecifier: "@azure/logger"
    });
    const name = (_b = (_a = emitterOptions.options.packageDetails) === null || _a === void 0 ? void 0 : _a.nameWithoutScope) !== null && _b !== void 0 ? _b : (_c = emitterOptions.options.packageDetails) === null || _c === void 0 ? void 0 : _c.name;
    sourceFile.addStatements(`export const logger = createClientLogger("${name}");`);
}
//# sourceMappingURL=emitLoggerFile.js.map