import { StructureKind } from "ts-morph";
import { resolveReference } from "../framework/reference.js";
import { isReadOnly } from "@azure-tools/typespec-client-generator-core";
import { isAzurePackage, NameType, normalizeName } from "@azure-tools/rlc-common";
import { useContext } from "../contextManager.js";
import { join } from "path";
import { AzureIdentityDependencies } from "../modular/external-dependencies.js";
import { reportDiagnostic } from "../index.js";
import { NoTarget } from "@typespec/compiler";
import { buildPropertyNameMapper, isSpreadBodyParameter } from "./helpers/typeHelpers.js";
import { getClassicalClientName } from "./helpers/namingHelpers.js";
import { hasKeyCredential, hasTokenCredential } from "../utils/credentialUtils.js";
import { getMethodHierarchiesMap, isTenantLevelOperation } from "../utils/operationUtil.js";
import { getSubscriptionId } from "../transform/transfromRLCOptions.js";
import { getClientParametersDeclaration, getClientParameters, hasDefaultValue, getClientParameterName } from "./helpers/clientHelpers.js";
import { getOperationFunction } from "./helpers/operationHelpers.js";
/**
 * Helpers to emit samples
 */
export function emitSamples(dpgContext) {
    const generatedFiles = [];
    const clients = dpgContext.sdkPackage.clients;
    for (const client of dpgContext.sdkPackage.clients) {
        emitClientSamples(dpgContext, client, {
            topLevelClient: client,
            generatedFiles,
            subFolder: clients.length > 1
                ? normalizeName(getClassicalClientName(client), NameType.File)
                : undefined
        });
    }
    return generatedFiles;
}
function emitClientSamples(dpgContext, client, options) {
    const methodMap = getMethodHierarchiesMap(dpgContext, client);
    for (const [prefixKey, operations] of methodMap) {
        const hierarchies = prefixKey ? prefixKey.split("/") : [];
        const prefix = hierarchies
            .map((name) => {
            return normalizeName(name, NameType.Property);
        })
            .join(".");
        for (const op of operations) {
            emitMethodSamples(dpgContext, op, {
                ...options,
                classicalMethodPrefix: prefix,
                hierarchies: hierarchies
            });
        }
    }
}
function emitMethodSamples(dpgContext, method, options) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q, _r;
    const examples = (_a = method.operation.examples) !== null && _a !== void 0 ? _a : [];
    if (examples.length === 0) {
        return;
    }
    const project = useContext("outputProject");
    const operationPrefix = `${(_b = options.classicalMethodPrefix) !== null && _b !== void 0 ? _b : ""} ${(_c = method.oriName) !== null && _c !== void 0 ? _c : method.name}`;
    const sampleFolder = join((_e = (_d = dpgContext.generationPathDetail) === null || _d === void 0 ? void 0 : _d.rootDir) !== null && _e !== void 0 ? _e : "", "samples-dev", (_f = options.subFolder) !== null && _f !== void 0 ? _f : "");
    const fileName = normalizeName(`${operationPrefix} Sample`, NameType.File);
    const sourceFile = project.createSourceFile(join(sampleFolder, `${fileName}.ts`), "", {
        overwrite: true
    });
    const exampleFunctions = [];
    // TODO: remove hard-coded for package
    if ((_h = (_g = dpgContext.rlcOptions) === null || _g === void 0 ? void 0 : _g.packageDetails) === null || _h === void 0 ? void 0 : _h.name) {
        sourceFile.addImportDeclaration({
            moduleSpecifier: (_k = (_j = dpgContext.rlcOptions) === null || _j === void 0 ? void 0 : _j.packageDetails) === null || _k === void 0 ? void 0 : _k.name,
            namedImports: [getClassicalClientName(options.topLevelClient)]
        });
    }
    for (const example of examples) {
        const exampleFunctionBody = [];
        const exampleName = normalizeName(escapeSpecialCharToSpace(example.name), NameType.Method);
        const exampleFunctionType = {
            name: exampleName,
            returnType: "Promise<void>",
            body: exampleFunctionBody
        };
        const parameterMap = buildParameterValueMap(example);
        const parameters = prepareExampleParameters(dpgContext, method, parameterMap, options.topLevelClient);
        // prepare client-level parameters
        const clientParamValues = parameters.filter((p) => p.onClient);
        const clientParams = clientParamValues
            .filter((p) => !p.isOptional)
            .map((param) => {
            exampleFunctionBody.push(`const ${param.name} = ${param.value};`);
            return param.name;
        });
        const optionalClientParams = clientParamValues
            .filter((p) => p.isOptional)
            .map((param) => `${param.name}: ${param.value}`);
        if (optionalClientParams.length > 0) {
            exampleFunctionBody.push(`const clientOptions = {${optionalClientParams.join(", ")}};`);
            clientParams.push("clientOptions");
        }
        exampleFunctionBody.push(`const client = new ${getClassicalClientName(options.topLevelClient)}(${clientParams.join(", ")});`);
        // prepare operation-level parameters
        // Get the actual function signature parameter order
        const operationFunction = getOperationFunction(dpgContext, [(_l = options.hierarchies) !== null && _l !== void 0 ? _l : [], method], "Client");
        // Extract parameter names from the function signature (excluding context and options)
        const signatureParamNames = (_o = (_m = operationFunction.parameters) === null || _m === void 0 ? void 0 : _m.filter((p) => {
            var _a;
            return p.name !== "context" &&
                !((_a = p.type) === null || _a === void 0 ? void 0 : _a.toString().includes("OptionalParams"));
        }).map((p) => p.name)) !== null && _o !== void 0 ? _o : [];
        const methodParamValues = parameters.filter((p) => !p.onClient);
        // Create a map for quick lookup of parameter values by name
        const paramValueMap = new Map(methodParamValues.map((p) => [p.name, p]));
        // Reorder methodParamValues according to the signature order
        const orderedRequiredParams = signatureParamNames
            .map((name) => paramValueMap.get(name))
            .filter((p) => p !== undefined && !p.isOptional);
        const methodParams = orderedRequiredParams.map((p) => `${p.value}`);
        const optionalParams = methodParamValues
            .filter((p) => p.isOptional)
            .map((param) => `${param.name}: ${param.value}`);
        if (optionalParams.length > 0) {
            methodParams.push(`{${optionalParams.join(", ")}}`);
        }
        const prefix = options.classicalMethodPrefix
            ? `${options.classicalMethodPrefix}.`
            : "";
        const isPaging = method.kind === "paging";
        const methodCall = `client.${prefix}${normalizeName((_p = method.oriName) !== null && _p !== void 0 ? _p : method.name, NameType.Property)}(${methodParams.join(", ")})`;
        if (isPaging) {
            exampleFunctionBody.push(`const resArray = new Array();`);
            exampleFunctionBody.push(`for await (const item of ${methodCall}) { resArray.push(item); }`);
            exampleFunctionBody.push(`console.log(resArray);`);
        }
        else if (method.response.type === undefined) {
            // skip response handling for void methods
            exampleFunctionBody.push(`await ${methodCall};`);
        }
        else {
            exampleFunctionBody.push(`const result = await ${methodCall};`);
            exampleFunctionBody.push(`console.log(result);`);
        }
        // Create a function declaration structure
        const description = (_q = method.doc) !== null && _q !== void 0 ? _q : `execute ${(_r = method.oriName) !== null && _r !== void 0 ? _r : method.name}`;
        const normalizedDescription = description.charAt(0).toLowerCase() + description.slice(1);
        const functionDeclaration = {
            returnType: exampleFunctionType.returnType,
            kind: StructureKind.Function,
            isAsync: true,
            name: exampleFunctionType.name,
            statements: exampleFunctionType.body,
            docs: [
                `This sample demonstrates how to ${normalizedDescription}\n\n@summary ${normalizedDescription}\nx-ms-original-file: ${example.filePath}`
            ]
        };
        sourceFile.addFunction(functionDeclaration);
        exampleFunctions.push(exampleFunctionType.name);
    }
    // Add statements referencing the tracked declarations
    const functions = exampleFunctions.map((f) => `await ${f}();`).join("\n");
    sourceFile.addStatements(`
  async function main(): Promise<void> {
    ${functions}
  }

  main().catch(console.error);`);
    options.generatedFiles.push(sourceFile);
    return sourceFile;
}
function buildParameterValueMap(example) {
    const parameterMap = {};
    example.parameters.forEach((param) => (parameterMap[param.parameter.serializedName] = param));
    return parameterMap;
}
function prepareExampleValue(context, name, value, isOptional, onClient) {
    return {
        name: normalizeName(name, NameType.Parameter, true),
        value: typeof value === "string" ? value : getParameterValue(context, value),
        isOptional: Boolean(isOptional),
        onClient: Boolean(onClient)
    };
}
function prepareExampleParameters(dpgContext, method, parameterMap, topLevelClient) {
    var _a;
    // TODO: blocked by TCGC issue: https://github.com/Azure/typespec-azure/issues/1419
    // refine this to support generic client-level parameters once resolved
    const result = [];
    // Get the raw SDK parameters to check for default values
    const rawClientParams = getClientParameters(topLevelClient, dpgContext, {
        onClientOnly: true
    });
    const clientParams = getClientParametersDeclaration(topLevelClient, dpgContext, {
        onClientOnly: true
    });
    // Helper to check if a parameter has a default value
    const hasParamDefaultValue = (paramName) => {
        const rawParam = rawClientParams.find((p) => {
            return getClientParameterName(p) === paramName;
        });
        if (!rawParam)
            return false;
        return hasDefaultValue(rawParam);
    };
    for (const param of clientParams) {
        if (param.name === "options" || param.name === "credential") {
            continue;
        }
        // Skip parameters that have default values - they are truly optional
        if (hasParamDefaultValue(param.name)) {
            continue;
        }
        const exampleValue = {
            name: param.name === "endpointParam" ? "endpoint" : param.name,
            value: getEnvironmentVariableName(param.name, getClassicalClientName(topLevelClient)),
            isOptional: Boolean(param.hasQuestionToken),
            onClient: true
        };
        result.push(exampleValue);
    }
    const credentialExampleValue = getCredentialExampleValue(dpgContext, topLevelClient.clientInitialization);
    if (credentialExampleValue) {
        result.push(credentialExampleValue);
    }
    let subscriptionIdValue = `"00000000-0000-0000-0000-000000000000"`;
    let isSubscriptionIdAdded = false;
    // required parameters
    for (const param of method.operation.parameters) {
        if (param.optional === true ||
            param.type.kind === "constant" ||
            param.clientDefaultValue) {
            continue;
        }
        const exampleValue = parameterMap[param.serializedName];
        // Handle subscriptionId parameter separately for ARM clients
        // Add it as long as it's the parameter of the method, even no example provided
        if (param.name.toLowerCase() === "subscriptionid" && dpgContext.arm) {
            isSubscriptionIdAdded = true;
            if (exampleValue && exampleValue.value) {
                subscriptionIdValue = getParameterValue(dpgContext, exampleValue.value);
            }
            result.push(prepareExampleValue(dpgContext, param.name, subscriptionIdValue, param.optional, param.onClient));
            continue;
        }
        if (!exampleValue || !exampleValue.value) {
            // report diagnostic if required parameter is missing
            reportDiagnostic(dpgContext.program, {
                code: "required-sample-parameter",
                format: {
                    exampleName: (_a = method.oriName) !== null && _a !== void 0 ? _a : method.name,
                    paramName: param.name
                },
                target: NoTarget
            });
            continue;
        }
        result.push(prepareExampleValue(dpgContext, exampleValue.parameter.name, exampleValue.value, param.optional, param.onClient));
    }
    // If client-level subscriptionId is needed on the client for this method, then add it
    // For example, Operations_List
    if (dpgContext.arm &&
        getSubscriptionId(dpgContext) &&
        !isSubscriptionIdAdded &&
        !isTenantLevelOperation(method, topLevelClient)) {
        result.push(prepareExampleValue(dpgContext, "subscriptionId", subscriptionIdValue, false, true));
    }
    // required/optional body parameters
    const bodyParam = method.operation.bodyParam;
    const bodySerializeName = bodyParam === null || bodyParam === void 0 ? void 0 : bodyParam.serializedName;
    const bodyExample = parameterMap[bodySerializeName !== null && bodySerializeName !== void 0 ? bodySerializeName : ""];
    if (bodyParam && bodyExample && bodyExample.value) {
        if (isSpreadBodyParameter(bodyParam) &&
            bodyParam.type.kind === "model" &&
            bodyExample.value.kind === "model") {
            for (const prop of bodyParam.type.properties) {
                const propExample = bodyExample.value.value[prop.name];
                if (!propExample) {
                    continue;
                }
                // Skip readonly properties as they cannot be set by users
                if (isReadOnly(prop)) {
                    continue;
                }
                result.push(prepareExampleValue(dpgContext, prop.name, propExample, prop.optional, prop.onClient));
            }
        }
        else {
            result.push(prepareExampleValue(dpgContext, bodyParam.name, bodyExample.value, bodyParam.optional, bodyParam.onClient));
        }
    }
    // optional parameters
    method.operation.parameters
        .filter((param) => param.optional === true &&
        parameterMap[param.serializedName] &&
        !param.clientDefaultValue)
        .map((param) => parameterMap[param.serializedName])
        .forEach((param) => {
        result.push(prepareExampleValue(dpgContext, param.parameter.name, param.value, true, param.parameter.onClient));
    });
    return result;
}
function getCredentialExampleValue(dpgContext, initialization) {
    const keyCredential = hasKeyCredential(initialization), tokenCredential = hasTokenCredential(initialization);
    const defaultSetting = {
        isOptional: false,
        onClient: true,
        name: "credential"
    };
    if (keyCredential || tokenCredential) {
        if (isAzurePackage({ options: dpgContext.rlcOptions })) {
            // Support DefaultAzureCredential for Azure packages
            return {
                ...defaultSetting,
                value: `new ${resolveReference(AzureIdentityDependencies.DefaultAzureCredential)}()`
            };
        }
        else if (keyCredential) {
            // Support ApiKeyCredential for non-Azure packages
            return {
                ...defaultSetting,
                value: `{ key: "INPUT_YOUR_KEY_HERE" }`
            };
        }
        else if (tokenCredential) {
            // Support TokenCredential for non-Azure packages
            return {
                ...defaultSetting,
                value: `{ getToken: async () => {
          return { token: "INPUT_YOUR_TOKEN_HERE", expiresOnTimestamp: now() }; } }`
            };
        }
    }
    return undefined;
}
function getParameterValue(context, value, options) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k;
    let retValue = `{} as any`;
    switch (value.kind) {
        case "string": {
            switch (value.type.kind) {
                case "plainDate":
                case "utcDateTime":
                    retValue = `new Date("${value.value}")`;
                    break;
                case "bytes": {
                    const encode = (_a = value.type.encode) !== null && _a !== void 0 ? _a : "base64";
                    // TODO: add check for un-supported encode
                    retValue = `Buffer.from("${value.value}",  "${encode}")`;
                    break;
                }
                default:
                    retValue = `"${(_b = value.value) === null || _b === void 0 ? void 0 : _b.toString().replace(/\\/g, "\\\\").replace(/"/g, '\\"').replace(/\n/g, "\\n").replace(/\r/g, "\\r").replace(/\t/g, "\\t").replace(/\f/g, "\\f").replace(/>/g, ">").replace(/</g, "<")}"`;
                    break;
            }
            break;
        }
        case "boolean":
        case "number":
        case "unknown":
        case "union":
            retValue = `${JSON.stringify(value.value)}`;
            break;
        case "null": {
            const ignoreNullableOnOptional = (_d = (_c = context.rlcOptions) === null || _c === void 0 ? void 0 : _c.ignoreNullableOnOptional) !== null && _d !== void 0 ? _d : false;
            if (ignoreNullableOnOptional) {
                // When ignore-nullable-on-optional is true, the TypeScript type won't include
                // | null for optional properties, so we convert null to a type-appropriate default
                // to avoid type errors in the generated sample code.
                const innerTypeKind = value.type.type.kind;
                if (innerTypeKind === "array") {
                    retValue = "[]";
                }
                else if (innerTypeKind === "model" || innerTypeKind === "dict") {
                    retValue = "{}";
                }
                else {
                    retValue = "undefined";
                }
            }
            else {
                retValue = `${JSON.stringify(value.value)}`;
            }
            break;
        }
        case "dict":
        case "model": {
            const mapper = buildPropertyNameMapper(context, value.type, options === null || options === void 0 ? void 0 : options.overrides);
            const values = [];
            const additionalPropertiesValue = value.kind === "model" ? ((_e = value.additionalPropertiesValue) !== null && _e !== void 0 ? _e : {}) : {};
            for (const propName in {
                ...value.value
            }) {
                let property;
                if (value.type.kind === "model") {
                    property = value.type.properties.find((p) => p.name === propName);
                }
                const propValue = value.value[propName];
                if (propValue === undefined || propValue === null) {
                    continue;
                }
                // Skip readonly properties as they cannot be set by users
                if (property && isReadOnly(property)) {
                    continue;
                }
                let propRetValue;
                if ((property === null || property === void 0 ? void 0 : property.flatten) &&
                    property.type.kind === "model" &&
                    ((_f = options === null || options === void 0 ? void 0 : options.overrides) === null || _f === void 0 ? void 0 : _f.enableFlatten) !== false) {
                    // For flatten property, we need to recursively get its properties
                    // but disable further flattening to match the TypeScript interface structure
                    const paramValue = getParameterValue(context, propValue, {
                        overrides: {
                            propertyRenames: (_g = useContext("sdkTypes").flattenProperties.get(property)) === null || _g === void 0 ? void 0 : _g.conflictMap,
                            enableFlatten: false
                        }
                    });
                    propRetValue =
                        paramValue.length > 2 ? paramValue.slice(1, -1) : undefined;
                }
                else {
                    // Don't propagate enableFlatten:false to deeper levels — it's only
                    // meant to block consecutive (transition) flatten at the direct child
                    // level.  Non-flatten properties should recurse with default behavior
                    // so that independent inner flattens at deeper levels still work.
                    const childOptions = ((_h = options === null || options === void 0 ? void 0 : options.overrides) === null || _h === void 0 ? void 0 : _h.enableFlatten) === false ? undefined : options;
                    propRetValue =
                        `"${(_j = mapper.get(propName)) !== null && _j !== void 0 ? _j : propName}": ` +
                            getParameterValue(context, propValue, childOptions);
                }
                if (propRetValue)
                    values.push(propRetValue);
            }
            const additionalBags = [];
            for (const propName in {
                ...additionalPropertiesValue
            }) {
                const propValue = additionalPropertiesValue[propName];
                if (propValue === undefined || propValue === null) {
                    continue;
                }
                const propRetValue = `"${(_k = mapper.get(propName)) !== null && _k !== void 0 ? _k : propName}": ` +
                    getParameterValue(context, propValue);
                additionalBags.push(propRetValue);
            }
            if (additionalBags.length > 0) {
                const name = mapper.get("additionalProperties")
                    ? "additionalPropertiesBag"
                    : "additionalProperties";
                values.push(`"${name}": {
          ${additionalBags.join(", ")}
          }`);
            }
            retValue = `{${values.join(", ")}}`;
            break;
        }
        case "array": {
            const valuesArr = value.value.map((item) => getParameterValue(context, item));
            retValue = `[${valuesArr.join(", ")}]`;
            break;
        }
        default:
            break;
    }
    return retValue;
}
function escapeSpecialCharToSpace(str) {
    if (!str) {
        return str;
    }
    return str.replace(/_|,|\.|\(|\)|'s |\[|\]/g, " ").replace(/\//g, " Or ");
}
function getEnvironmentVariableName(paramName, clientName) {
    // Remove "Param" suffix if present
    const cleanName = paramName.replace(/Param$/, "");
    // Remove "Client" suffix from client name if present and convert to UPPER_SNAKE_CASE
    let prefix = "";
    if (clientName) {
        const cleanClientName = clientName.replace(/Client$/, "");
        prefix =
            cleanClientName
                .replace(/([A-Z])/g, "_$1")
                .toUpperCase()
                .replace(/^_/, "") + "_";
    }
    // Convert camelCase to UPPER_SNAKE_CASE
    const envVarName = cleanName
        .replace(/([A-Z])/g, "_$1")
        .toUpperCase()
        .replace(/^_/, "");
    return `process.env.${prefix}${envVarName} || ""`;
}
//# sourceMappingURL=emitSamples.js.map