import { SdkClientType, SdkServiceOperation } from "@azure-tools/typespec-client-generator-core";
import { ModularEmitterOptions } from "./interfaces.js";
import { Node, SourceFile } from "ts-morph";
export interface buildSubpathIndexFileOptions {
    exportIndex?: boolean;
    interfaceOnly?: boolean;
    recursive?: boolean;
}
export declare function buildSubpathIndexFile(emitterOptions: ModularEmitterOptions, subpath: string, clientMap?: [string[], SdkClientType<SdkServiceOperation>], options?: buildSubpathIndexFileOptions): void;
export declare function isTypeOnlyNode(node: Node): boolean;
/**
 * Partition declaration entries into type-only and value exports in a single pass,
 * then emit both export declarations on the index file.
 */
export declare function partitionAndEmitExports(indexFile: SourceFile, moduleSpecifier: string, entries: [string, Node[]][], mapName?: (name: string) => string): void;
//# sourceMappingURL=buildSubpathIndex.d.ts.map