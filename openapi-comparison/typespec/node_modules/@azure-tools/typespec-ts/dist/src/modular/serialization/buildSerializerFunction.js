import { StructureKind } from "ts-morph";
import { UsageFlags } from "@azure-tools/typespec-client-generator-core";
import { getAllAncestors, getAllProperties, getPropertyFullName, getRequestModelMapping, getSerializationExpression } from "../helpers/operationHelpers.js";
import { getAdditionalPropertiesName, normalizeModelName } from "../emitModels.js";
import { NameType, normalizeName } from "@azure-tools/rlc-common";
import { isAzureCoreErrorType } from "../../utils/modelUtils.js";
import { getAllDiscriminatedValues, isDiscriminatedUnion, isSupportedSerializeType } from "./serializeUtils.js";
import { MultipartHelpers, SerializationHelpers } from "../static-helpers-metadata.js";
import { resolveReference } from "../../framework/reference.js";
import { isOrExtendsHttpFile } from "@typespec/http";
import { refkey } from "../../framework/refkey.js";
import { getAdditionalPropertiesType, getDirectSubtypes } from "../helpers/typeHelpers.js";
import { reportDiagnostic } from "../../lib.js";
import { NoTarget } from "@typespec/compiler";
import { useContext } from "../../contextManager.js";
export function buildPropertySerializer(context, property, options = {
    nameOnly: false,
    skipDiscriminatedUnionSuffix: false
}) {
    const propertyContext = useContext("sdkTypes").flattenProperties.get(property);
    // only build de-serializer for flatten property
    if (property.flatten !== true || !propertyContext) {
        return undefined;
    }
    const predefinedName = `_${normalizeName(`${propertyContext.baseModel.name}_${property.name}`, NameType.Method, true)}Serializer`;
    return buildModelSerializer(context, property.type, {
        ...options,
        flatten: {
            baseModel: propertyContext.baseModel,
            property
        },
        overrides: {
            allOptional: property.optional,
            propertyRenames: propertyContext.conflictMap
        },
        predefinedName
    });
}
export function buildModelSerializer(context, type, options = {
    nameOnly: false,
    skipDiscriminatedUnionSuffix: false
}) {
    if (!isSupportedSerializeType(type)) {
        return undefined;
    }
    const { nameOnly } = options;
    if (type.kind === "model" || type.kind === "union" || type.kind === "enum") {
        if (!type.usage ||
            (type.usage !== undefined &&
                (type.usage & UsageFlags.Input) !== UsageFlags.Input)) {
            return undefined;
        }
        if (!type.name) {
            // TODO: https://github.com/Azure/typespec-azure/issues/1713 and https://github.com/microsoft/typespec/issues/4815
            // throw new Error(`NYI Serialization of anonymous types`);
            return undefined;
        }
        if (isAzureCoreErrorType(context.program, type.__raw) ||
            isOrExtendsHttpFile(context.program, type.__raw)) {
            return undefined;
        }
    }
    if (!isDiscriminatedUnion(type) &&
        type.kind === "model" &&
        type.discriminatorProperty) {
        return buildPolymorphicSerializer(context, type, nameOnly);
    }
    if (isDiscriminatedUnion(type) && !options.skipDiscriminatedUnionSuffix) {
        return buildDiscriminatedUnionSerializer(context, type, nameOnly);
    }
    switch (type.kind) {
        case "model":
            return buildModelTypeSerializer(context, type, options);
        case "union": // for non-discriminated union, we just return whatever we get
            return buildUnionSerializer(context, type, nameOnly);
        case "dict":
            return buildDictTypeSerializer(context, type, nameOnly);
        case "array":
            return buildArrayTypeSerializer(context, type, nameOnly);
        default:
            return undefined;
    }
}
function buildPolymorphicSerializer(context, type, nameOnly = false) {
    if (!type.name) {
        reportDiagnostic(context.program, {
            code: "anonymous-type-serialization",
            target: type.__raw || NoTarget
        });
        return undefined; // Return undefined to skip this serialization
    }
    const serializeFunctionName = `${normalizeModelName(context, type, NameType.Operation)}Serializer`;
    if (nameOnly) {
        return resolveReference(refkey(type, "serializer"));
    }
    const serializerFunction = {
        kind: StructureKind.Function,
        name: serializeFunctionName,
        isExported: true,
        parameters: [
            {
                name: "item",
                type: resolveReference(refkey(type, "polymorphicType"))
            }
        ],
        returnType: "any",
        statements: []
    };
    if (!type.discriminatorProperty) {
        return;
    }
    const statements = [];
    const subTypes = type.discriminatedSubtypes;
    if (!subTypes) {
        // No subtypes - treat as a regular model and generate simple serializer
        return buildModelTypeSerializer(context, type, {
            nameOnly,
            skipDiscriminatedUnionSuffix: false
        });
    }
    const cases = [];
    Object.keys(subTypes).forEach((discriminatedValue) => {
        const subType = subTypes[discriminatedValue];
        if (!(subType === null || subType === void 0 ? void 0 : subType.usage) ||
            ((subType === null || subType === void 0 ? void 0 : subType.usage) !== undefined &&
                (subType.usage & UsageFlags.Input) !== UsageFlags.Input)) {
            return;
        }
        const union = (subType === null || subType === void 0 ? void 0 : subType.discriminatedSubtypes) ? "_Union" : "";
        if (!subType || !(subType === null || subType === void 0 ? void 0 : subType.name)) {
            reportDiagnostic(context.program, {
                code: "anonymous-type-serialization",
                target: (subType === null || subType === void 0 ? void 0 : subType.__raw) || NoTarget
            });
            return; // Skip this subtype
        }
        const rawSubTypeName = `${subType.name}${union}`;
        const subTypeName = `${normalizeName(rawSubTypeName, NameType.Interface, true)}`;
        const subtypeSerializerName = normalizeName(`${rawSubTypeName}_Serializer`, NameType.Method, true);
        cases.push(`
        case "${discriminatedValue}":
          return ${subtypeSerializerName}(item as ${subTypeName});
      `);
    });
    statements.push(`
      switch (item.${normalizeName(type.discriminatorProperty.name, NameType.Property)}) {
       ${cases.join("\n")}
        default:
          return item;
      }
    `);
    serializerFunction.statements = statements.join("\n");
    return serializerFunction;
}
function buildDiscriminatedUnionSerializer(context, type, nameOnly = false) {
    if (!type.name) {
        reportDiagnostic(context.program, {
            code: "anonymous-type-serialization",
            target: type.__raw || NoTarget
        });
        return undefined; // Return undefined to skip this serialization
    }
    const cases = [];
    const output = [];
    const serializeFunctionName = `${normalizeModelName(context, type, NameType.Operation)}Serializer`;
    if (nameOnly) {
        return resolveReference(refkey(type, "serializer"));
    }
    // Get the base serializer name and ensure reference tracking
    const baseSerializerName = `${normalizeModelName(context, type, NameType.Operation, true)}Serializer`;
    const directSubtypes = getDirectSubtypes(type);
    for (const subType of directSubtypes) {
        if (!subType.usage ||
            (subType.usage !== undefined &&
                (subType.usage & UsageFlags.Input) !== UsageFlags.Input)) {
            continue;
        }
        // get all discriminated values that is linked by this discriminator property
        const discriminatedValues = getAllDiscriminatedValues(subType, type.discriminatorProperty);
        const union = subType.discriminatedSubtypes ? "Union" : "";
        const subTypeName = normalizeModelName(context, subType, NameType.Interface, !union);
        // Get the serializer name and ensure reference tracking
        const subtypeSerializerName = buildModelSerializer(context, subType, {
            nameOnly: true,
            skipDiscriminatedUnionSuffix: false
        });
        const caseLabels = discriminatedValues.map((value) => `case "${value}":`);
        cases.push(`
      ${caseLabels.join("\n")}
        return ${subtypeSerializerName}(item as ${subTypeName});
    `);
    }
    output.push(`
    switch (item.${type.discriminatorProperty ? normalizeName(type.discriminatorProperty.name, NameType.Property) : "unknown"}) {
     ${cases.join("\n")}
      default:
        return ${baseSerializerName}(item);
    }
  `);
    const serializerFunction = {
        kind: StructureKind.Function,
        name: serializeFunctionName,
        isExported: true,
        parameters: [
            {
                name: "item",
                type: resolveReference(refkey(type, "polymorphicType"))
            }
        ],
        returnType: "any",
        statements: output.join("\n")
    };
    return serializerFunction;
}
function buildUnionSerializer(context, type, nameOnly = false) {
    if (!type.name) {
        reportDiagnostic(context.program, {
            code: "anonymous-type-serialization",
            target: type.__raw || NoTarget
        });
        return ""; // Return empty string to continue processing
    }
    const serializerFunctionName = `${normalizeModelName(context, type, NameType.Operation)}Serializer`;
    if (nameOnly) {
        return resolveReference(refkey(type, "serializer"));
    }
    const serializerFunction = {
        kind: StructureKind.Function,
        name: serializerFunctionName,
        isExported: true,
        parameters: [
            {
                name: "item",
                type: resolveReference(refkey(type))
            }
        ],
        returnType: "any",
        statements: ["return item;"]
    };
    return serializerFunction;
}
function buildModelTypeSerializer(context, type, options = {
    nameOnly: false,
    skipDiscriminatedUnionSuffix: false
}) {
    var _a, _b, _c, _d;
    if (!type.name) {
        reportDiagnostic(context.program, {
            code: "anonymous-type-deserialization",
            target: type.__raw || NoTarget
        });
        return ""; // Return empty string to continue processing
    }
    const serializerFunctionName = (_a = options.predefinedName) !== null && _a !== void 0 ? _a : `${normalizeModelName(context, type, NameType.Operation, options.skipDiscriminatedUnionSuffix)}Serializer`;
    if (options.nameOnly) {
        return options.flatten
            ? resolveReference(refkey(options.flatten.property, "serializer"))
            : resolveReference(refkey(type, "serializer"));
    }
    const serializerFunction = {
        kind: StructureKind.Function,
        name: serializerFunctionName,
        isExported: true,
        parameters: [
            {
                name: "item",
                type: options.flatten
                    ? resolveReference(refkey(options.flatten.baseModel))
                    : resolveReference(refkey(type))
            }
        ],
        returnType: "any",
        statements: ["return item;"]
    };
    if ((type.usage & UsageFlags.Input) === UsageFlags.Input &&
        (type.usage & UsageFlags.MultipartFormData) === UsageFlags.MultipartFormData) {
        // For MFD models, serialize into an array of parts
        // TODO: cleaner abstraction, quite a bit of duplication with the non-MFD stuff here
        const parts = [];
        const properties = getAllProperties(context, type, getAllAncestors(type));
        for (const property of properties) {
            if (property.kind !== "property") {
                continue;
            }
            const expr = getSerializationExpression(context, property, "item");
            let partDefinition;
            const multipart = property.serializationOptions.multipart;
            if (multipart === null || multipart === void 0 ? void 0 : multipart.isFilePart) {
                const createFilePartDescriptorDefinition = resolveReference(MultipartHelpers.createFilePartDescriptor);
                const itemPath = multipart.isMulti
                    ? "x"
                    : getPropertyFullName(context, property, "item");
                partDefinition = `${createFilePartDescriptorDefinition}("${multipart.name}", ${itemPath}, )`;
                // If the TypeSpec doesn't specify a default content type, TCGC will infer a default of "*/*".
                // In this case, we actually want the content type to be left unset so that Core will take care of
                // setting the content type correctly.
                const contentType = ((_b = multipart.defaultContentTypes) === null || _b === void 0 ? void 0 : _b[0]) === "*/*"
                    ? undefined
                    : (_c = multipart.defaultContentTypes) === null || _c === void 0 ? void 0 : _c[0];
                const propertyAccessor = getPropertyFullName(context, property, "item");
                if (multipart.isMulti) {
                    partDefinition = `...(${propertyAccessor}.map((x: unknown) => ${createFilePartDescriptorDefinition}("${multipart.name}", x${contentType ? `,"${contentType}"` : ""})))`;
                }
                else {
                    partDefinition = `${createFilePartDescriptorDefinition}("${multipart.name}", ${propertyAccessor}${contentType ? `, "${contentType}"` : ""})`;
                }
            }
            else if (multipart === null || multipart === void 0 ? void 0 : multipart.isMulti) {
                partDefinition = `...((${expr}).map((x: unknown) => ({ name: "${multipart === null || multipart === void 0 ? void 0 : multipart.name}", body: x })))`;
            }
            else {
                partDefinition = `{ name: "${multipart === null || multipart === void 0 ? void 0 : multipart.name}", body: (${expr}) }`;
            }
            if (property.optional) {
                parts.push(`...(${getPropertyFullName(context, property, "item")} === undefined ? [] : [${partDefinition}])`);
            }
            else {
                parts.push(partDefinition);
            }
            // TODO: How to handle additionalProperties for MFD?
        }
        serializerFunction.statements = [`return [${parts.join(",")}]`];
    }
    else {
        const additionalPropertiesSpread = getAdditionalPropertiesStatement(context, type);
        const propertiesStr = getRequestModelMapping(context, type, "item", options.overrides, !options.flatten);
        if (additionalPropertiesSpread) {
            propertiesStr.unshift(additionalPropertiesSpread);
        }
        const serializeContent = `{${propertiesStr.join(",")}}`;
        const output = [];
        // don't emit a serializer if there is nothing to serialize
        if (propertiesStr.length) {
            output.push(`
        return ${serializeContent}
      `);
        }
        else if (options.flatten) {
            // Private flatten serializer: all child properties are read-only, so nothing to serialize.
            // Rename the parameter to _item to avoid TypeScript unused-variable errors.
            const firstParam = (_d = serializerFunction.parameters) === null || _d === void 0 ? void 0 : _d[0];
            if (firstParam) {
                firstParam.name = "_item";
            }
            output.push(`
        return {};
      `);
        }
        else {
            output.push(`
        return item;
      `);
        }
        serializerFunction.statements = output;
    }
    return serializerFunction;
}
function getAdditionalPropertiesStatement(context, type) {
    var _a;
    const additionalPropertyType = getAdditionalPropertiesType(type);
    if (!additionalPropertyType) {
        return undefined;
    }
    const deserializerFunction = buildModelSerializer(context, additionalPropertyType, {
        nameOnly: true,
        skipDiscriminatedUnionSuffix: false
    });
    const params = [`item.${getAdditionalPropertiesName(context, type)} ?? {}`];
    if (typeof deserializerFunction === "string") {
        params.push("undefined");
        params.push(deserializerFunction);
    }
    return ((_a = context.rlcOptions) === null || _a === void 0 ? void 0 : _a.compatibilityMode) === true
        ? "...item"
        : `...${resolveReference(SerializationHelpers.serializeRecord)}(${params.join(",")})`;
}
function buildDictTypeSerializer(context, type, nameOnly = false) {
    var _a;
    const valueSerializer = buildModelSerializer(context, type.valueType, {
        nameOnly: true,
        skipDiscriminatedUnionSuffix: false
    });
    if (!valueSerializer) {
        return undefined;
    }
    if (!isSupportedSerializeType(type.valueType)) {
        return undefined;
    }
    if (typeof valueSerializer !== "string") {
        return undefined;
    }
    const serializerFunctionName = `${normalizeModelName(context, type, NameType.Operation, false, true)}Serializer`;
    if (nameOnly) {
        return resolveReference(refkey(type, "serializer"));
    }
    const serializerFunction = {
        kind: StructureKind.Function,
        name: serializerFunctionName,
        isExported: true,
        parameters: [
            {
                name: "item",
                type: `Record<string, ${(_a = resolveReference(type.valueType)) !== null && _a !== void 0 ? _a : "any"}>`
            }
        ],
        returnType: "Record<string, any>",
        statements: [
            `
  const result: Record<string, any> = {};
  Object.keys(item).map((key) => {
    result[key] = !item[key]? item[key]: ${valueSerializer}(item[key])
  });
  return result;
      `
        ]
    };
    return serializerFunction;
}
function buildArrayTypeSerializer(context, type, nameOnly = false) {
    var _a;
    const valueSerializer = buildModelSerializer(context, type.valueType, {
        nameOnly: true,
        skipDiscriminatedUnionSuffix: false
    });
    if (!valueSerializer) {
        return undefined;
    }
    if (!isSupportedSerializeType(type.valueType)) {
        return undefined;
    }
    if (typeof valueSerializer !== "string") {
        return undefined;
    }
    const serializerFunctionName = `${normalizeModelName(context, type, NameType.Operation, false, true)}Serializer`;
    if (nameOnly) {
        return resolveReference(refkey(type, "serializer"));
    }
    const serializerFunction = {
        kind: StructureKind.Function,
        name: serializerFunctionName,
        isExported: true,
        parameters: [
            {
                name: "result",
                type: `Array<${(_a = normalizeModelName(context, type.valueType)) !== null && _a !== void 0 ? _a : "any"}>`
            }
        ],
        returnType: "any[]",
        statements: [
            `
  return result.map((item) => {
    return ${valueSerializer}(item)
  });
      `
        ]
    };
    return serializerFunction;
}
//# sourceMappingURL=buildSerializerFunction.js.map