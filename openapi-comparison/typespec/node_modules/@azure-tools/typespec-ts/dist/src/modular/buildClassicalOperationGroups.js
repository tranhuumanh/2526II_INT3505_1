import { NameType } from "@azure-tools/rlc-common";
import { getClassicalOperation } from "./helpers/classicalOperationHelpers.js";
import { getClassicalLayerPrefix } from "./helpers/namingHelpers.js";
import { getMethodHierarchiesMap } from "../utils/operationUtil.js";
import { getModularClientOptions } from "../utils/clientUtils.js";
import { useContext } from "../contextManager.js";
export function buildClassicOperationFiles(dpgContext, clientMap, emitterOptions) {
    var _a, _b;
    // const sdkPackage = dpgContext.sdkPackage;
    const project = useContext("outputProject");
    const [_hierarchy, client] = clientMap;
    const { subfolder } = getModularClientOptions(clientMap);
    const classicOperationFiles = new Map();
    const methodMap = getMethodHierarchiesMap(dpgContext, client);
    for (const [prefixKey, operations] of methodMap) {
        const prefixes = prefixKey.split("/");
        if (prefixes.length > 0 && prefixKey !== "") {
            const classicOperationFileName = prefixes.length > 0
                ? `${getClassicalLayerPrefix(prefixes, NameType.File, "/", prefixes.length - 1)}/index`
                : // When the program has no operation groups defined all operations are put
                    // into a nameless operation group. We'll call this operations.
                    "index";
            const srcPath = emitterOptions.modularOptions.sourceRoot;
            const classicFile = (_a = classicOperationFiles.get(classicOperationFileName)) !== null && _a !== void 0 ? _a : project.createSourceFile(`${srcPath}/${subfolder && subfolder !== "" ? subfolder + "/" : ""}classic/${classicOperationFileName}.ts`);
            getClassicalOperation(dpgContext, clientMap, classicFile, [
                prefixes,
                operations
            ]);
            classicOperationFiles.set(classicOperationFileName, classicFile);
        }
    }
    for (const [prefixKey, operations] of methodMap) {
        const prefixes = prefixKey.split("/");
        if (prefixes.length > 0 && prefixKey !== "") {
            for (let layer = 0; layer < prefixes.length - 1; layer++) {
                const classicOperationFileName = prefixes.length > 0
                    ? `${getClassicalLayerPrefix(prefixes, NameType.File, "/", layer)}/index`
                    : // When the program has no operation groups defined all operations are put
                        // into a nameless operation group. We'll call this operations.
                        "index";
                const srcPath = emitterOptions.modularOptions.sourceRoot;
                const classicFile = (_b = classicOperationFiles.get(classicOperationFileName)) !== null && _b !== void 0 ? _b : project.createSourceFile(`${srcPath}/${subfolder && subfolder !== "" ? subfolder + "/" : ""}classic/${classicOperationFileName}.ts`);
                getClassicalOperation(dpgContext, clientMap, classicFile, [prefixes, operations], layer);
                classicOperationFiles.set(classicOperationFileName, classicFile);
            }
        }
    }
    return classicOperationFiles;
}
//# sourceMappingURL=buildClassicalOperationGroups.js.map