import { resolveReference } from "../../framework/reference.js";
import { getTypeExpression } from "./get-type-expression.js";
import { shouldEmitInline } from "./utils.js";
export function getUnionExpression(context, type, options = {}) {
    if (shouldEmitInline(type, options)) {
        const variantTypes = new Set(type.variantTypes.map((v) => `${getTypeExpression(context, v, options)}`));
        return `(${[...variantTypes].join(" | ")})`;
    }
    else {
        return resolveReference(type);
    }
}
//# sourceMappingURL=get-union-expression.js.map