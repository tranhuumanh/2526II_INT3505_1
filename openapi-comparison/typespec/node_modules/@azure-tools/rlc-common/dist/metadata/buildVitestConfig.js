"use strict";
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildVitestConfig = buildVitestConfig;
const nodeConfig = `
import viteConfig from "../../../vitest.shared.config.ts";

export default viteConfig;`;
const browserConfig = `
import viteConfig from "../../../vitest.browser.shared.config.ts";

export default viteConfig;`;
const esmConfig = `
import { mergeConfig } from "vitest/config";
import vitestConfig from "./vitest.config.ts";
import vitestEsmConfig from "../../../vitest.esm.shared.config.ts";

export default mergeConfig(
  vitestConfig,
  vitestEsmConfig
);`;
function buildVitestConfig(model, platform) {
    var _a, _b;
    if (((_a = model.options) === null || _a === void 0 ? void 0 : _a.generateMetadata) === false ||
        ((_b = model.options) === null || _b === void 0 ? void 0 : _b.generateTest) === false) {
        return;
    }
    switch (platform) {
        case "browser":
            return {
                path: "vitest.browser.config.ts",
                content: browserConfig
            };
        case "node":
            return {
                path: "vitest.config.ts",
                content: nodeConfig
            };
        case "esm":
            return {
                path: "vitest.esm.config.ts",
                content: esmConfig
            };
    }
}
//# sourceMappingURL=buildVitestConfig.js.map